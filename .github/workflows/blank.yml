jobs:
  check-ips:
    runs-on: ubuntu-latest
    steps:
      - name: Compare IPs
        run: |
          echo "This is the GitHub Runner's IP: $(curl -s https://ifconfig.me)"
          echo "This is MY actual Public IP: ${{ secrets.MY_OFFICE_IP }}"
